# Local development database used by the Prisma CLI (migrate / generate /
# studio / seed). The packaged app does NOT read this — it keeps its database in
# the OS per-user app-data folder (see Settings > About for the exact path).
DATABASE_URL="file:./prisma/dev.db"
